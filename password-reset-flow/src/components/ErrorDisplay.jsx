// ErrorDisplay.jsx
import React from 'react';

const ErrorDisplay = ({ errorMessage }) => {
    // Logic for displaying error messages
    return (
        // JSX for displaying error messages
        <div>
            <p>{errorMessage}</p>
        </div>
    );
};

export default ErrorDisplay;
