// EmailService.js
// Placeholder for sending emails (using external service or API)
const EmailService = {
  sendResetEmail: (email) => {
    // Logic to send reset email
    console.log(`Sending reset email to ${email}`);
  },
};

export default EmailService;
